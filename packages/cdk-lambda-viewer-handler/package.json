{
  "name": "ses-mail-catcher-cdk-lambda-viewer-handler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/copy-viewer-assets.mjs",
    "compile": "tsc -p tsconfig.build.json && node scripts/copy-viewer-assets.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "oxlint": "^1.81.0",
    "postal-mime": "^3.0.0",
    "ses-mail-catcher-api-contract": "workspace:*",
    "ses-mail-catcher-viewer": "workspace:*",
    "typescript": "~7.0.2",
    "vitest": "^5.0.0"
  }
}

# Workspace definition for the pnpm + Turborepo monorepo.

packages:
  - packages/*

onlyBuiltDependencies:
  - "@parcel/watcher"
  - unrs-resolver
allowBuilds:
  "@parcel/watcher": true
  unrs-resolver: true

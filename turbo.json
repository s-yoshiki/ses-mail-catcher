{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".oxlintrc.json"],
  "tasks": {
    "build": {
      "outputs": ["lib/**", "dist/**"]
    },
    "compile": {
      "outputs": ["lib/**", ".jsii"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "typecheck": {
      "outputs": []
    },
    "package": {
      "dependsOn": ["build"],
      "outputs": ["dist/**"]
    },
    "clobber": {
      "cache": false
    }
  }
}

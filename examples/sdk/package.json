{
  "name": "ses-mail-catcher-sdk-example",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "compile": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "oxlint src",
    "send": "pnpm run build && node lib/send-mail.js"
  },
  "dependencies": {
    "@aws-sdk/client-sesv2": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "oxlint": "^1.81.0",
    "typescript": "~7.0.2"
  }
}

{
  "name": "cdk-ses-mail-catcher-workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "compile": "turbo run compile",
    "package": "turbo run package",
    "clean": "turbo run clobber",
    "projen": "pnpm --filter cdk-ses-mail-catcher projen"
  },
  "devDependencies": {
    "oxlint": "^1.81.0",
    "turbo": "^2.5.8"
  },
  "packageManager": "pnpm@10.33.0"
}
